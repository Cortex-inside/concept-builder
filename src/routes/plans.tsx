import { createFileRoute,Link } from "@tanstack/react-router"; import { Check } from "lucide-react"; import { plans } from "../lib/concept-data"; export const Route=createFileRoute("/plans")({component:Plans}); function Plans(){return <main className="mx-auto max-w-6xl px-4 py-12"><div className="mx-auto max-w-2xl text-center"><p className="text-sm font-semibold uppercase text-blue-600">Planos</p><h1 className="mt-1 text-4xl font-bold">Planos para diferentes necessidades</h1><p className="mt-3 text-slate-600">Comece gratuitamente e evolua com a sua empresa.</p></div><div className="mt-10 grid gap-5 md:grid-cols-3">{plans.map(p=><article key={p.name} className="rounded-3xl border bg-white p-7"><h2 className="text-xl font-bold">{p.name}</h2><p className="mt-2 text-sm text-slate-500">{p.description}</p><p className="mt-6 text-3xl font-extrabold">{p.price}</p><ul className="mt-6 space-y-3">{p.features.map(f=><li key={f} className="flex gap-2 text-sm"><Check size={18} className="text-blue-600"/>{f}</li>)}</ul><Link to="/register" className="mt-8 block rounded-xl bg-blue-600 py-3 text-center font-semibold text-white">Começar</Link></article>)}</div></main>}