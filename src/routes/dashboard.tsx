import { createFileRoute,Link } from "@tanstack/react-router"; import { Building2,FileCheck2,FileText } from "lucide-react"; import { requests } from "../lib/concept-data"; export const Route=createFileRoute("/dashboard")({component:Dashboard}); function Dashboard(){return <main className="mx-auto max-w-7xl px-4 py-12"><div className="flex flex-wrap justify-between gap-4"><div><p className="text-sm font-semibold uppercase text-blue-600">Área empresarial</p><h1 className="text-3xl font-bold">Painel</h1><p className="mt-2 text-slate-600">Gerencie perfil, pedidos e propostas.</p></div><div className="flex gap-3"><Link to="/dashboard/profile" className="rounded-xl border bg-white px-4 py-2 font-semibold">Editar perfil</Link><Link to="/requests/new" className="rounded-xl bg-blue-600 px-4 py-2 font-semibold text-white">Novo pedido</Link></div></div><div className="mt-8 grid gap-4 md:grid-cols-3">{[[Building2,"Perfil","75% completo"],[FileText,"Pedidos","3 activos"],[FileCheck2,"Propostas","9 recebidas"]].map(([I,a,b])=><div key={a as string} className="rounded-2xl border bg-white p-6"><I as any className="text-blue-600"/><p className="mt-5 text-sm text-slate-500">{a as string}</p><p className="text-2xl font-bold">{b as string}</p></div>)}</div><section className="mt-8 rounded-2xl border bg-white p-6"><h2 className="text-xl font-bold">Pedidos recentes</h2>{requests.map(r=><div key={r.id} className="flex justify-between border-b py-4"><span><b>{r.title}</b><small className="block text-slate-500">{r.sector} · {r.province}</small></span><span className="text-sm text-blue-700">{r.proposals} propostas</span></div>)}</section></main>}