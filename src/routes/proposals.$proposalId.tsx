import { createFileRoute,Link } from "@tanstack/react-router"; import { useState } from "react"; export const Route=createFileRoute("/proposals/$proposalId")({component:SubmitProposal}); function SubmitProposal(){const[a,setA]=useState(""),[d,setD]=useState(""),[n,setN]=useState(""),[sent,setSent]=useState(false);if(sent)return <main className="mx-auto max-w-2xl px-4 py-16 text-center"><div className="rounded-3xl border bg-white p-10"><h1 className="text-3xl font-bold">Proposta enviada</h1><p className="mt-3 text-slate-600">A proposta ficou disponível para comparação.</p><Link to="/proposals" className="mt-6 inline-block text-blue-700">Voltar</Link></div></main>;return <main className="mx-auto max-w-2xl px-4 py-12"><h1 className="text-3xl font-bold">Enviar proposta</h1><form onSubmit={e=>{e.preventDefault();setSent(true)}} className="mt-8 space-y-5 rounded-3xl border bg-white p-7"><label className="field-label">Valor<input required value={a} onChange={e=>setA(e.target.value)} className="field" placeholder="Ex.: 6.500.000 MZN"/></label><label className="field-label">Prazo<input required value={d} onChange={e=>setD(e.target.value)} className="field" placeholder="Ex.: 90 dias"/></label><label className="field-label">Observações<textarea value={n} onChange={e=>setN(e.target.value)} className="field min-h-32"/></label><button className="w-full rounded-xl bg-blue-600 py-3 font-semibold text-white">Enviar proposta</button></form></main>}