import { createFileRoute,Link,useNavigate } from "@tanstack/react-router"; import { useState } from "react"; export const Route=createFileRoute("/login")({component:Login}); function Login(){const[e,setE]=useState(""),[p,setP]=useState("");const nav=useNavigate();return <main className="mx-auto max-w-md px-4 py-16"><div className="rounded-3xl border bg-white p-8"><h1 className="text-3xl font-bold">Entrar</h1><p className="mt-2 text-slate-600">Aceda ao seu espaço empresarial.</p><form onSubmit={x=>{x.preventDefault();localStorage.setItem("concept-user",e);nav({to:"/dashboard"})}} className="mt-7 space-y-4"><label className="field-label">Email<input required type="email" value={e} onChange={x=>setE(x.target.value)} className="field"/></label><label className="field-label">Palavra-passe<input required type="password" value={p} onChange={x=>setP(x.target.value)} className="field"/></label><button className="w-full rounded-xl bg-blue-600 py-3 font-semibold text-white">Entrar</button></form><p className="mt-6 text-center text-sm">Ainda não tem conta? <Link to="/register" className="text-blue-700">Criar conta</Link></p></div></main>}