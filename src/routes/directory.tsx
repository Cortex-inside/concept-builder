import { createFileRoute, Link } from "@tanstack/react-router";
import { useMemo,useState } from "react";
import { CheckCircle2, MapPin, ArrowRight } from "lucide-react";
import { companies,provinces,sectors } from "../lib/concept-data";
import { PageHeader,SearchBox } from "../components/app-shell";
export const Route=createFileRoute("/directory")({component:Directory});
function Directory(){const[q,setQ]=useState(""),[sector,setSector]=useState(""),[province,setProvince]=useState(""),[verified,setVerified]=useState(false);const list=useMemo(()=>companies.filter(c=>(!q||[c.name,c.description,...c.services].join(" ").toLowerCase().includes(q.toLowerCase()))&&(!sector||c.sector===sector)&&(!province||c.province===province)&&(!verified||c.verified)),[q,sector,province,verified]);return <main className="mx-auto max-w-7xl px-4 py-10 lg:px-6"><PageHeader eyebrow="Diretório empresarial" title="Encontre fornecedores para o seu negócio" description="Pesquise empresas por actividade, localização e informação verificada."/><div className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><div className="grid gap-3 md:grid-cols-[1fr_190px_190px_auto]"><SearchBox value={q} onChange={setQ} placeholder="Pesquisar empresa, serviço ou produto"/><select className="field" value={sector} onChange={e=>setSector(e.target.value)}><option value="">Todos os sectores</option>{sectors.map(s=><option key={s}>{s}</option>)}</select><select className="field" value={province} onChange={e=>setProvince(e.target.value)}><option value="">Todas as províncias</option>{provinces.map(p=><option key={p}>{p}</option>)}</select><label className="flex min-h-11 items-center gap-2 rounded-lg border border-slate-200 px-3 text-sm font-medium text-slate-600"><input type="checkbox" checked={verified} onChange={e=>setVerified(e.target.checked)} className="accent-[#0f766e]"/> Verificadas</label></div></div><div className="my-7"><p className="text-sm font-semibold text-slate-500">{list.length} empresas encontradas</p></div><div className="grid gap-4 md:grid-cols-2">{list.map(c=><article key={c.id} className="group rounded-xl border border-slate-200 bg-white p-6 shadow-sm transition hover:-translate-y-0.5 hover:border-slate-300 hover:shadow-md"><div className="flex items-start justify-between gap-4"><div><div className="flex items-center gap-2"><h2 className="text-lg font-bold text-[#102a43]">{c.name}</h2>{c.verified&&<CheckCircle2 className="text-[#0f766e]" size={17}/>}</div><p className="mt-1 flex items-center gap-1 text-xs font-bold uppercase tracking-wide text-[#0f766e]"><MapPin size={13}/>{c.sector} · {c.province}</p></div></div><p className="mt-4 text-sm leading-6 text-slate-600">{c.description}</p><div className="mt-5 flex flex-wrap gap-2">{c.services.map(s=><span key={s} className="rounded-md bg-slate-100 px-2.5 py-1 text-xs font-medium text-slate-600">{s}</span>)}</div><Link to="/companies/$companyId" params={{companyId:c.id}} className="mt-6 flex items-center justify-between border-t border-slate-100 pt-4 text-sm font-bold text-[#0b5f59]">Ver perfil <ArrowRight size={16}/></Link></article>)}</div></main>}
