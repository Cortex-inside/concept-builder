import { createFileRoute,Link } from "@tanstack/react-router"; import { requests } from "../lib/concept-data"; export const Route=createFileRoute("/requests")({component:Requests}); function Requests(){return <main className="mx-auto max-w-6xl px-4 py-12"><div className="flex justify-between"><div><h1 className="text-3xl font-bold">Pedidos</h1><p className="mt-2 text-slate-600">Necessidades publicadas pela sua empresa.</p></div><Link to="/requests/new" className="rounded-xl bg-blue-600 px-4 py-2 font-semibold text-white">Criar pedido</Link></div><div className="mt-8 space-y-4">{requests.map(r=><article key={r.id} className="rounded-2xl border bg-white p-6"><h2 className="font-bold">{r.title}</h2><p className="mt-1 text-sm text-slate-500">{r.sector} · {r.province} · {r.budget}</p><p className="mt-3 text-sm text-slate-600">{r.description}</p><Link to="/proposals" className="mt-4 inline-block text-sm font-semibold text-blue-700">Ver fornecedores e propostas →</Link></article>)}</div></main>}