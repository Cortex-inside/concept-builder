import { createFileRoute,Link } from "@tanstack/react-router"; import { useState } from "react"; import { provinces,sectors } from "../lib/concept-data"; export const Route=createFileRoute("/requests/new")({component:NewRequest}); function NewRequest(){const[t,setT]=useState(""),[s,setS]=useState(sectors[0]),[p,setP]=useState(provinces[0]),[b,setB]=useState(""),[d,setD]=useState(""),[done,setDone]=useState(false);if(done)return <main className="mx-auto max-w-2xl px-4 py-16 text-center"><div className="rounded-3xl border bg-white p-10"><h1 className="text-3xl font-bold">Pedido criado</h1><p className="mt-3 text-slate-600">Fornecedores compatíveis estão disponíveis.</p><Link to="/proposals" className="mt-6 inline-block rounded-xl bg-blue-600 px-5 py-3 font-semibold text-white">Ver fornecedores</Link></div></main>;return <main className="mx-auto max-w-3xl px-4 py-12"><h1 className="text-3xl font-bold">O que precisa?</h1><p className="mt-2 text-slate-600">Descreva a necessidade para encontrar fornecedores.</p><form onSubmit={e=>{e.preventDefault();setDone(true)}} className="mt-8 space-y-5 rounded-3xl border bg-white p-7"><label className="field-label">Título<input required value={t} onChange={e=>setT(e.target.value)} className="field"/></label><div className="grid gap-4 md:grid-cols-2"><label className="field-label">Sector<select className="field" value={s} onChange={e=>setS(e.target.value)}>{sectors.map(x=><option key={x}>{x}</option>)}</select></label><label className="field-label">Província<select className="field" value={p} onChange={e=>setP(e.target.value)}>{provinces.map(x=><option key={x}>{x}</option>)}</select></label></div><label className="field-label">Orçamento<input value={b} onChange={e=>setB(e.target.value)} className="field"/></label><label className="field-label">Descrição<textarea required value={d} onChange={e=>setD(e.target.value)} className="field min-h-40"/></label><button className="w-full rounded-xl bg-blue-600 py-3 font-semibold text-white">Criar pedido</button></form></main>}