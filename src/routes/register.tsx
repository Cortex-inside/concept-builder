import { createFileRoute,Link,useNavigate } from "@tanstack/react-router"; import { useState } from "react"; export const Route=createFileRoute("/register")({component:Register}); function Register(){const[n,setN]=useState(""),[e,setE]=useState(""),[p,setP]=useState("");const nav=useNavigate();return <main className="mx-auto max-w-md px-4 py-16"><div className="rounded-3xl border bg-white p-8"><h1 className="text-3xl font-bold">Criar conta</h1><form onSubmit={x=>{x.preventDefault();localStorage.setItem("concept-company",n);localStorage.setItem("concept-user",e);nav({to:"/dashboard/profile"})}} className="mt-7 space-y-4"><label className="field-label">Nome da empresa<input required value={n} onChange={x=>setN(x.target.value)} className="field"/></label><label className="field-label">Email<input required type="email" value={e} onChange={x=>setE(x.target.value)} className="field"/></label><label className="field-label">Palavra-passe<input required type="password" value={p} onChange={x=>setP(x.target.value)} className="field"/></label><button className="w-full rounded-xl bg-blue-600 py-3 font-semibold text-white">Criar conta</button></form><p className="mt-6 text-center text-sm">Já tem conta? <Link to="/login" className="text-blue-700">Entrar</Link></p></div></main>}