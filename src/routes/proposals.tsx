import { createFileRoute,Link } from "@tanstack/react-router"; import { useState } from "react"; import { CheckCircle2,Mail,Send } from "lucide-react"; import { companies } from "../lib/concept-data"; export const Route=createFileRoute("/proposals")({component:Proposals}); function Proposals(){const[i,setI]=useState<string[]>([]);return <main className="mx-auto max-w-7xl px-4 py-12"><h1 className="text-3xl font-bold">Fornecedores e propostas</h1><p className="mt-2 text-slate-600">Convide fornecedores e compare propostas.</p><div className="mt-8 grid gap-4 md:grid-cols-2">{companies.slice(0,4).map(c=><div key={c.id} className="rounded-2xl border bg-white p-6"><div className="flex items-center gap-2"><h2 className="font-bold">{c.name}</h2>{c.verified&&<CheckCircle2 className="text-blue-600" size={18}/>}</div><p className="mt-2 text-sm text-blue-700">{c.sector}</p><p className="mt-3 text-sm text-slate-600">{c.description}</p><button onClick={()=>setI(v=>v.includes(c.id)?v:[...v,c.id])} className="mt-5 flex w-full items-center justify-center gap-2 rounded-xl border py-2.5 font-semibold">{i.includes(c.id)?<><Send size={16}/> Convite enviado</>:<><Mail size={16}/> Convidar para propor</>}</button></div>)}</div><section className="mt-10 rounded-2xl border bg-white p-6"><h2 className="text-xl font-bold">Comparação</h2><div className="mt-4 overflow-x-auto"><table className="w-full min-w-[600px] text-left text-sm"><thead><tr className="border-b"><th className="py-3">Fornecedor</th><th>Valor</th><th>Prazo</th><th>Estado</th></tr></thead><tbody>{companies.slice(0,3).map((c,n)=><tr key={c.id} className="border-b"><td className="py-4 font-semibold">{c.name}</td><td>{["6,2 M","7,1 M","5,8 M"][n]} MZN</td><td>{[90,75,100][n]} dias</td><td>Recebida</td></tr>)}</tbody></table></div><Link to="/proposals/prop-1" className="mt-5 inline-block font-semibold text-blue-700">Abrir formulário de proposta →</Link></section></main>}